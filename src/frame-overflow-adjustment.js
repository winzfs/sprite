(() => {
  // Sprite crop adjustment is now handled directly in src/app.js.
  // This file is intentionally kept as a no-op so old script references do not
  // re-override the core editor functions with stale behavior.
})();
