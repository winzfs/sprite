# AI 작업 시작 전 필독 문서

이 문서는 이 저장소에서 AI가 코드를 수정하거나 기능을 추가하기 전에 반드시 먼저 읽어야 하는 작업 지침이다.

## 1. 먼저 읽을 문서 순서

작업을 시작하기 전에 아래 순서로 확인한다.

1. `AI_README.md` — AI 작업 규칙, 금지사항, 우선순위
2. `docs/SPRITE_EDITOR.md` — 스프라이트 편집기 현재 구조, 기능 현황, 개발 방향
3. `index.html` — 현재 UI와 버튼 ID
4. `src/app.js` — 실제 앱 상태와 이벤트 연결
5. `styles.css` — 모바일/그리드/버튼 레이아웃
6. `src/grid-layout.js` — 출력 순서 그리드 열 수 고정 로직

문서를 읽지 않고 추측으로 수정하지 않는다.

## 2. 절대 원칙

### 기존 기능 삭제 금지

새 기능을 추가하더라도 기존 기능을 임의로 제거하면 안 된다.

특히 아래 기능은 항상 유지한다.

- 이미지 업로드
- 자동 추정
- 전체 균등
- 다시 분할
- 출력 순서 표시
- 선택 추가
- 전체 추가
- 선택 제거
- 출력 비우기
- 전체 선택
- 선택 해제
- 다중 선택
- 위치 보정
- 프레임 박스 크기 보정
- 크기 초기화
- 가운데 정렬
- 재생/정지
- 앞으로/뒤로 이동
- PNG 내보내기
- 배경색/투명 배경
- 모바일에서 원본 열 수 그대로 출력 순서 표시

기능을 제거해야 할 것 같으면 먼저 이유를 설명하고 허락을 받아야 한다.

### 버튼 ID 변경 주의

`index.html`의 버튼 ID와 `src/app.js`의 이벤트 연결은 반드시 함께 맞춰야 한다.

버튼을 추가하거나 이름을 바꾸면 다음을 확인한다.

- HTML에 버튼이 존재하는가?
- `app.js` 클릭 이벤트에서 해당 ID를 처리하는가?
- 모바일에서도 버튼이 보이는가?
- 기존 버튼과 기능이 충돌하지 않는가?

### 출력 순서 중심 UX 유지

현재 편집기의 핵심 작업 공간은 `출력 순서`다.

- 사용자는 출력 순서에서 프레임을 선택한다.
- 출력 순서에서 다중 선택한다.
- 출력 순서에서 위치 보정과 크기 보정을 적용한다.
- 재생과 PNG 출력은 출력 순서를 기준으로 한다.

`분할된 프레임` 목록은 현재 숨김 상태로 둘 수 있다. 단, 내부 데이터와 기능이 필요하면 유지해도 된다.

## 3. 현재 중요한 구현 규칙

### 상태 구조

현재 주요 상태는 `src/app.js`의 `state`에 있다.

- `frames`: 기본 분할 프레임
- `frameShifts`: 프레임별 원본 위치 보정값
- `frameSizes`: 프레임별 프레임 박스 크기 보정값
- `selectedFrameIds`: 선택된 프레임 ID 집합
- `outputFrameIds`: 출력 순서
- `selectedOutputIndex`: 출력 순서에서 선택된 인덱스

이 구조를 깨지 말고, 새 기능도 가능하면 이 구조에 맞춰 추가한다.

### 프레임 박스 크기 보정

프레임 박스 크기는 원본 이미지 크기보다 커질 수 있어야 한다.

중요:

- 끝 열/끝 행 프레임도 전체 선택 시 같은 크기로 늘어나야 한다.
- 원본 이미지 밖으로 확장된 영역은 잘리면 안 된다.
- 원본 밖 영역은 투명 또는 배경색으로 처리한다.
- PNG 출력 캔버스도 확장된 프레임 크기를 반영해야 한다.

### 출력 순서 열 수

모바일에서도 출력 순서는 원본 분할 설정의 열 수를 그대로 따라야 한다.

예:

```text
원본 열 수 = 6
출력 순서 = 6열 표시
```

6열이 5열로 줄어드는 회귀를 만들면 안 된다.

### 캔버스 렌더링

원본 이미지 밖으로 확장된 프레임을 그릴 때는 `drawImage`로 바로 전체 rect를 그리면 안 된다.

반드시 이미지와 겹치는 영역만 계산해서 그리고, 나머지 영역은 투명 또는 배경색으로 남겨야 한다.

현재 관련 함수:

- `getSourceRect(frame)`
- `getFrameIntersection(rect)`
- `drawFrameImage(ctx, frame, dx, dy, scale)`

이 함수들의 역할을 이해하고 수정한다.

## 4. Cloudflare Pages 배포 규칙

이 저장소는 빌드 없는 정적 배포를 기준으로 한다.

권장 Cloudflare Pages 설정:

```text
Framework preset: None
Build command: 비워두기
Build output directory: /
Root directory: /
```

JS/CSS를 수정하면 `index.html`의 쿼리 버전을 올린다.

예:

```html
<link rel="stylesheet" href="styles.css?v=22">
<script src="src/app.js?v=22" defer></script>
```

캐시 문제 때문에 버전을 올리지 않으면 사용자가 수정사항을 못 볼 수 있다.

## 5. 수정 전 확인 체크리스트

코드를 수정하기 전에 반드시 확인한다.

- [ ] 이 변경이 기존 기능을 삭제하지 않는가?
- [ ] 버튼 ID와 이벤트 연결이 맞는가?
- [ ] 모바일에서 출력 순서 열 수가 유지되는가?
- [ ] 프레임 박스가 원본 밖으로 확장되는 경우를 고려했는가?
- [ ] PNG 출력이 미리보기와 같은 기준을 쓰는가?
- [ ] Cloudflare 캐시 회피용 버전을 올려야 하는가?
- [ ] 관련 문서를 업데이트해야 하는가?

## 6. 수정 후 테스트 체크리스트

수정 후 최소한 아래는 확인한다.

- [ ] 이미지 업로드
- [ ] 자동 추정
- [ ] 다시 분할
- [ ] 출력 순서 표시
- [ ] 전체 선택
- [ ] 다중 선택
- [ ] 선택 추가
- [ ] 전체 추가
- [ ] 선택 제거
- [ ] 출력 비우기
- [ ] 위치 보정
- [ ] 프레임 박스 너비/높이 보정
- [ ] 끝 열 프레임 크기 동일성
- [ ] 원본 밖 확장 영역 보존
- [ ] 재생
- [ ] PNG 내보내기
- [ ] 모바일 6열 표시

## 7. 개발 방향

단기 목표:

1. 현재 단일 `app.js` 안정화
2. 기존 기능 회귀 방지
3. 모바일 출력 순서 UX 안정화
4. 프레임 박스 확장/출력 로직 안정화

중기 목표:

1. `app.js`를 기능별 파일로 분리
2. Undo/Redo 추가
3. 프로젝트 설정 JSON 저장/불러오기
4. 자동 추정 정확도 개선
5. 단축키 지원

장기 목표:

1. Pixi.js 기반 게임 제작 워크플로우와 연결하기 쉬운 메타데이터 출력
2. 애니메이션 행/액션 단위 관리
3. 여러 스프라이트 시트 일괄 처리
4. 웹 기반 에셋 파이프라인 도구로 확장

## 8. AI 응답 원칙

- 모르는 상태에서 확신하지 않는다.
- 저장소 상태를 먼저 확인한다.
- 사용자가 말한 기능을 임의로 축소 해석하지 않는다.
- 이전 기능을 없애지 않는다.
- 큰 변경은 전체 흐름을 보고 적용한다.
- 임시 땜질보다 구조적으로 덜 깨지는 방식을 택한다.
- 작업 후 어떤 파일을 바꿨고 어떤 기능을 유지/추가했는지 짧게 보고한다.
